To reproduce,   need to run

1. S mClustersT etEmbedd ngAdhocApp to generate cluster -> top t ets and t et -> top clusters data sets
2. T etS m lar yEvaluat onSampl ngAdhocApp to sample a subset of t ets that   want to compute so   tr cs on
3. T etS m lar yEvaluat onAdhocApp to perform t  evaluat on
